#include <stdio.h>

/**
 * main - Entry point of the program
 * Return: return value of the main function
 */

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}

	putchar('\n');

	return (0);
}
